# Copy this file to .env and fill in your key.
# Only needed for the "Ask the Data" text-to-SQL dashboard page — everything
# else in this project (dbt, the other dashboard pages) works without it.

# Anthropic (Claude) API key — get one at https://console.anthropic.com
ANTHROPIC_API_KEY=your_anthropic_key_here
